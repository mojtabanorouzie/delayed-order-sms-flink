{
  "scenarioName": "mixed-orders",
  "description": "Generates a configurable mix of all supported order scenarios.",
  "topic": "Orders",
  "topicType": "compacted",
  "keyField": "orderId",
  "defaultOrderCount": 100,
  "distribution": {
    "onTimePercentage": 35,
    "delayedPercentage": 25,
    "cancelledPercentage": 15,
    "etaUpdatedPercentage": 10,
    "duplicatePercentage": 10,
    "outOfOrderPercentage": 5
  },
  "includedScenarios": [
    "on-time-orders",
    "delayed-orders",
    "cancelled-orders",
    "eta-updated-orders",
    "duplicate-events",
    "out-of-order-updates"
  ],
  "expectedResult": {
    "description": "The Flink job should correctly process a mixed workload. SMS commands should be emitted based on whether orders are delayed (past-dated ETAs).",
    "smsCommandExpected": true,
    "validationRule": "SMS count should match delayed orders count (25% of 100 = 25 expected, but accounting for cancellations and eta updates, typically ~20-25)."
  }
}